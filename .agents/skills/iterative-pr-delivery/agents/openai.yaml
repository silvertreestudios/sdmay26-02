interface:
  display_name: "Iterative PR Delivery"
  short_description: "Deliver PRs through one-time local and remote review gates"
  default_prompt: "Use $iterative-pr-delivery to implement this issue through one-time local review, iterative automated review, CI, and human handoff."
